name: demo actions
on: 
  push:
    branches: git_actions

jobs:
  build:
    name: demo job run 
    runs-on: ubuntu-latest
    steps:
      - name: run sh commands
        run: |
          python --version 
          echo "Hello World!"
      - uses: actions/checkout@v2
      - name: run py script
        run: |
          python ./demo-actions/demo_py_script.py
      - name: run sh script
        run: |
          chmod +x ./demo-actions/demo_sh_script.sh
          ./demo-actions/demo_sh_script.sh
      - name: cml demo
        



